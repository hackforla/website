# This workflow generates new issues
name: "CodeQL Create Issues"

on:  
  schedule:
    - cron: '30 5 * * 5'
  workflow_dispatch:

jobs:
  codql-scan-job:
    uses: ./.github/workflows/codeql-scan-script.yml
  create-new-issues-job:
    needs: codql-scan-job
    uses: ./.github/workflows/codeql-create-issues-script.yml