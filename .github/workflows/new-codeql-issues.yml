# This workflow generates new issues
name: "New CodeQL Issues"

on:
  #TODO: Remove push
  push:
    branches: [ 'refactor-codql-yml-6691' ]
  schedule:
    - cron: '30 5 * * 5'
  workflow_dispatch:

jobs:
  codql-scan-job:
    uses: ./.github/workflows/codeql-scripts/codeql-scan-script.yml
  create-new-issues-job:
    uses: ./.github/workflows/codeql-scripts/codeql-create-issues-script.yml